<section>
      <div class="row">

        <% @bidds.each do |bidd| %>
              <p>event: <%= bidd.event_id %></p>
              <p>Price: <%= bidd.bid %></p>
            
        <% end %>
      </div>
    </section>

