<section>
      <div class="row">
     <%= bar_chart @bidds.group(:bid).count %>

        <% @bidds.each do |bidd| %>
              <p>event: <%= bidd.event_id %></p>
              <p>Price: <%= bidd.bid %></p>
            
        <% end %>
      </div>
    </section>

